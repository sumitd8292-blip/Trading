Subject: FNO Market Depth — Both REST and WebSocket Feed fail (GA001 / NATS connection error)

Hi Groww API Support Team,

I have an active Trading API subscription and am building a personal algo-trading tool. I've been unable to get live market depth (order book) for FNO options working via EITHER of your two documented methods, despite extensive troubleshooting. Details below — happy to share full logs if useful.

======================
METHOD 1: REST /v1/live-data/quote and /v1/live-data/ltp
======================
Both consistently return:
{"status":"FAILURE","error":{"code":"GA001","message":"Bad Request. Please check your request parameters","metadata":null}}

Tried with:
- trading_symbol="NIFTY26AUG24200CE" (confirmed correct — matches BOTH the "trading_symbol" field in your own Option Chain API response for this exact contract, AND the format in your Instruments CSV documentation)
- Same symbol via /v1/live-data/ltp with exchange_symbols=["NSE_NIFTY26AUG24200CE"]
- exchange_token="61647" (looked up directly from your official instruments CSV at growwapi-assets.groww.in/instruments/instrument.csv) tried as the trading_symbol parameter value

All fail identically. The Option Chain API (GET /v1/option-chain/exchange/NSE/underlying/NIFTY) works perfectly for the exact same contract — LTP, OI, Greeks, volume all return correctly.

======================
METHOD 2: WebSocket Feed (growwapi Python SDK, GrowwFeed class)
======================
Using the official growwapi Python package:

from growwapi import GrowwFeed, GrowwAPI
groww = GrowwAPI(api_key)
feed = GrowwFeed(groww)
feed.subscribe_market_depth([{"exchange": "NSE", "segment": "FNO", "exchange_token": "61647"}], on_data_received=callback)

Observed sequence:
1. GrowwAPI init succeeds ("Ready to Groww!")
2. Socket token creation succeeds: POST /v1/api/apex/v1/socket/token/create returns 200
3. NATS connection attempt to wss://socket-api.groww.in begins
4. subscribe_market_depth() then logs repeated generic "Error:" (growwapi.groww.nats_client logger) with no further detail, even with DEBUG-level logging enabled — the underlying exception appears to stringify to empty

I used exchange_token="61647" (NIFTY, 25-Aug-2026 expiry, 24200 strike CE) — confirmed correct via your own instruments CSV, and via GrowwMCP's own successful depth-fetch for this exact token (which DOES return full 5-level bid/ask data when called through the MCP layer — so the token and contract are valid, live, and have depth data available).

QUESTIONS:
1. Is there a separate account-level permission/enablement needed specifically for Level-2 market depth (distinct from Option Chain/Greeks/candle access), that might not be active on my account?
2. For the WebSocket Feed — is there a way to get more detailed error output from the NATS connection/auth step? The library only logs a generic "Error:" with no exception detail.
3. Could you check server-side logs for socket-api.groww.in around [DATE/TIME OF YOUR TEST] for my account to see what's actually failing the NATS handshake/subscription?

My account email: [YOUR GROWW ACCOUNT EMAIL HERE]
API Key (last 4 chars only): [LAST 4 CHARS]
Test exchange_token used: 61647 (NIFTY 25-Aug-2026 24200 CE)

Thank you for your help — happy to hop on a call or share more logs if useful.

Regards,
Saim
